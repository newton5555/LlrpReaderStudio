<UserControl x:Class="LlrpReaderStudio.Views.ReaderUnavailableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LlrpReaderStudio.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:ReaderUnavailableViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36" MaxWidth="650" HorizontalAlignment="Left">
            <TextBlock Text="Reader Unavailable" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="{Binding ReaderName}" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,0" />
            <TextBlock Text="This reader could not be reached during startup, so its configuration is not available and the settings page is not shown." TextWrapping="Wrap" Margin="0,12,0,0" Foreground="#888888" />
            <TextBlock Text="{Binding ErrorMessage}" TextWrapping="Wrap" Margin="0,12,0,0" Foreground="#C62828" />

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Retry" Command="{Binding RetryCommand}" Padding="18,8" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
