<UserControl x:Class="LlrpReaderStudio.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LlrpReaderStudio.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:AboutViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36" MaxWidth="720" HorizontalAlignment="Left">
            
            <Border BorderThickness="1" BorderBrush="{DynamicResource MahApps.Brushes.Gray7}" CornerRadius="4" Background="{DynamicResource MahApps.Brushes.ThemeBackground}" Padding="32">
                <StackPanel>
                    <TextBlock Text="{Binding AppName}" FontSize="32" FontWeight="SemiBold" Foreground="{DynamicResource MahApps.Brushes.Accent}" />
                    <TextBlock Text="{Binding Version}" FontSize="14" Foreground="{DynamicResource MahApps.Brushes.Gray3}" Margin="0,4,0,0" />

                    <Separator Margin="0,16" Background="{DynamicResource MahApps.Brushes.Gray7}" />
                    
                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="15" LineHeight="24" Margin="0,0,0,24" />
                    
                    <Border Background="{DynamicResource MahApps.Brushes.Gray10}" Padding="16" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="License / Notice" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <TextBlock Text="{Binding LicenseNotice}" TextWrapping="Wrap" FontSize="13" LineHeight="22" Foreground="{DynamicResource MahApps.Brushes.Gray3}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>
