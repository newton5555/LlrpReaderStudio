<UserControl x:Class="LlrpReaderStudio.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LlrpReaderStudio.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:AboutViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36" MaxWidth="720" HorizontalAlignment="Left">
            <Border BorderThickness="1" Padding="28">
                <StackPanel>
                    <TextBlock Text="{Binding AppName}" FontSize="28" FontWeight="Bold" />
                    <TextBlock Text="{Binding Version}" FontSize="14" Margin="0,4,0,20" />
                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="14" LineHeight="24" />
                    <TextBlock Text="License / Notice" FontSize="16" FontWeight="Bold" Margin="0,24,0,8" />
                    <TextBlock Text="{Binding LicenseNotice}" TextWrapping="Wrap" FontSize="13" LineHeight="22" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
