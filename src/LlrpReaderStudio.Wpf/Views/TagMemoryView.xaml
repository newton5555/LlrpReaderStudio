<UserControl x:Class="LlrpReaderStudio.Views.TagMemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LlrpReaderStudio.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:TagMemoryViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#0F172A">
        <StackPanel Margin="36" MaxWidth="820" HorizontalAlignment="Left">
            <TextBlock Text="Tag Memory" FontSize="28" FontWeight="Bold" Foreground="#F8FAFC" />
            <TextBlock Text="Target one exact EPC. Reads and writes use a temporary SDK-managed AccessSpec." TextWrapping="Wrap" Foreground="#94A3B8" Margin="0,6,0,20" />

            <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="24" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Target EPC (hex)" FontWeight="Bold" Foreground="#F8FAFC" Margin="0,0,0,6" />
                    <TextBox Text="{Binding TargetEpc, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,18" Background="#0F172A" Foreground="#38BDF8" FontFamily="Consolas, Courier New" FontWeight="Bold" BorderBrush="#334155" />

                    <UniformGrid Columns="4" Margin="0,0,0,18">
                        <StackPanel Margin="0,0,8,0"><TextBlock Text="Memory bank" FontWeight="Bold" Foreground="#F8FAFC" /><ComboBox ItemsSource="{Binding MemoryBanks}" SelectedItem="{Binding MemoryBank}" Padding="8" Margin="0,6,0,0" Background="#0F172A" Foreground="#F8FAFC" /></StackPanel>
                        <StackPanel Margin="4,0,4,0"><TextBlock Text="Word pointer" FontWeight="Bold" Foreground="#F8FAFC" /><TextBox Text="{Binding WordPointer}" Padding="8" Margin="0,6,0,0" Background="#0F172A" Foreground="#F8FAFC" BorderBrush="#334155" /></StackPanel>
                        <StackPanel Margin="4,0,4,0"><TextBlock Text="Word count" FontWeight="Bold" Foreground="#F8FAFC" /><TextBox Text="{Binding WordCount}" Padding="8" Margin="0,6,0,0" Background="#0F172A" Foreground="#F8FAFC" BorderBrush="#334155" /></StackPanel>
                        <StackPanel Margin="8,0,0,0"><TextBlock Text="Access password" FontWeight="Bold" Foreground="#F8FAFC" /><TextBox Text="{Binding AccessPassword}" Padding="8" Margin="0,6,0,0" Background="#0F172A" Foreground="#F8FAFC" BorderBrush="#334155" /></StackPanel>
                    </UniformGrid>

                    <TextBlock Text="Data (hex words)" FontWeight="Bold" Foreground="#F8FAFC" Margin="0,0,0,6" />
                    <TextBox Text="{Binding TagData, UpdateSourceTrigger=PropertyChanged}" Height="120" AcceptsReturn="True" TextWrapping="Wrap" Padding="10" Margin="0,0,0,20" Background="#0F172A" Foreground="#F8FAFC" FontFamily="Consolas, Courier New" BorderBrush="#334155" />

                    <StackPanel Orientation="Horizontal">
                        <Button Content="READ MEMORY" Command="{Binding ReadMemoryCommand}" Style="{StaticResource WorkbenchButtonStyle}" Background="#0284C7" Foreground="White" Padding="18,8" />
                        <Button Content="WRITE MEMORY" Command="{Binding WriteMemoryCommand}" Style="{StaticResource WorkbenchButtonStyle}" Margin="12,0,0,0" Padding="18,8" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
