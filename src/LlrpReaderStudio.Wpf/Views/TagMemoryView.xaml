<UserControl x:Class="LlrpReaderStudio.Views.TagMemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LlrpReaderStudio.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:TagMemoryViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36" MaxWidth="820" HorizontalAlignment="Left">
            <TextBlock Text="Tag Memory" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MahApps.Brushes.Accent}" />
            <TextBlock Text="Target one exact EPC. Reads and writes use a temporary SDK-managed AccessSpec." TextWrapping="Wrap" Margin="0,6,0,24" Foreground="{DynamicResource MahApps.Brushes.Gray3}" FontSize="14" />
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,12" FontSize="14">
                <Run Text="Operating reader: " FontWeight="SemiBold" />
                <Run Text="{Binding OperationReaderName, Mode=OneWay}" />
            </TextBlock>

            <Border BorderThickness="1" BorderBrush="{DynamicResource MahApps.Brushes.Gray7}" CornerRadius="4" Padding="24" Margin="0,0,0,24" Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
                <StackPanel>
                    <UniformGrid Columns="2" Margin="0,0,0,8">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Target type" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <ComboBox Text="{Binding TargetType}" IsEditable="False" Padding="10">
                                <ComboBoxItem Content="EPC" />
                                <ComboBoxItem Content="TID" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="Match (hex)" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <TextBox Text="{Binding TargetMatch, UpdateSourceTrigger=PropertyChanged}" 
                                     mah:TextBoxHelper.Watermark="e.g. 300833B2DDD9014000000000"
                                     mah:TextBoxHelper.ClearTextButton="True"
                                     Padding="10" FontFamily="Consolas, Courier New" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>
                    </UniformGrid>

                    <UniformGrid Columns="4" Margin="0,0,0,20">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Memory bank" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <ComboBox ItemsSource="{Binding MemoryBankOptions}" SelectedItem="{Binding SelectedMemoryBank}" DisplayMemberPath="Display" Padding="8" />
                        </StackPanel>
                        <StackPanel Margin="4,0,4,0">
                            <TextBlock Text="Word pointer" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <TextBox Text="{Binding WordPointer}" Padding="8" mah:TextBoxHelper.Watermark="Offset" />
                        </StackPanel>
                        <StackPanel Margin="4,0,4,0">
                            <TextBlock Text="Word count" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <TextBox Text="{Binding WordCount}" Padding="8" mah:TextBoxHelper.Watermark="Length" />
                        </StackPanel>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="Access password" FontWeight="SemiBold" Margin="0,0,0,8" />
                            <TextBox Text="{Binding AccessPassword}" Padding="8" mah:TextBoxHelper.Watermark="00000000" FontFamily="Consolas, Courier New" />
                        </StackPanel>
                    </UniformGrid>

                    <TextBlock Text="Data (hex words)" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBox Text="{Binding TagData, UpdateSourceTrigger=PropertyChanged}" 
                             Height="140" AcceptsReturn="True" TextWrapping="Wrap" 
                             mah:TextBoxHelper.Watermark="Data read from or to be written to the tag..."
                             Padding="12" Margin="0,0,0,24" 
                             FontFamily="Consolas, Courier New" FontSize="14" />

                    <StackPanel Orientation="Horizontal">
                        <Button Content="READ MEMORY" Command="{Binding ReadMemoryCommand}" 
                                Style="{DynamicResource MahApps.Styles.Button.MetroSquare.Accent}"
                                MinWidth="140" Padding="16,10" FontWeight="SemiBold" />
                        <Button Content="WRITE MEMORY" Command="{Binding WriteMemoryCommand}" 
                                Margin="16,0,0,0" MinWidth="140" Padding="16,10" FontWeight="SemiBold" />
                    </StackPanel>

                    <Border BorderThickness="1" BorderBrush="{DynamicResource MahApps.Brushes.Gray7}" CornerRadius="4" Padding="12" Margin="0,20,0,0" Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
                        <TextBlock Text="{Binding OperationResultText, Mode=OneWay}" TextWrapping="Wrap" FontFamily="Consolas, Courier New" FontSize="14" />
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
