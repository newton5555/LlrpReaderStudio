<UserControl x:Class="LlrpReaderStudio.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LlrpReaderStudio.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36" MaxWidth="850" HorizontalAlignment="Left">
            <TextBlock Text="Application Settings" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="Software-level settings for LLRP Reader Studio. Reader-specific settings are opened by selecting a data source." TextWrapping="Wrap" Margin="0,6,0,20" />

            <GroupBox Header="Tag Logging" Padding="16" Margin="0,0,0,16">
                <StackPanel>
                    <CheckBox Content="Enable tag logging" IsChecked="{Binding EnableTagLogging}" Margin="0,0,0,8" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Log directory:" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Text="{Binding TagLogDirectory}" />
                    </Grid>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Application" Padding="16">
                <StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
